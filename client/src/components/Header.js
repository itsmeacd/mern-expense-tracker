import React from 'react';
import Typography from '@material-ui/core/Typography';

export const Header = () => {
    return (
        <Typography variant="h4" align="center" gutterBottom>Mern Expense Tracker</Typography>
    )
}